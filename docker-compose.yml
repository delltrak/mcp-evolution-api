services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "4899"
    environment:
      - NODE_ENV=production
      - PORT=4899
    restart: unless-stopped
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge 